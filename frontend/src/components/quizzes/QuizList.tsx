'use client';

// Placeholder — implement quiz list with delete in the next phase.

export function QuizList() {
  return (
    <div className="emptyState">
      <p>Quiz list component — connect to GET /api/quizzes.</p>
    </div>
  );
}
