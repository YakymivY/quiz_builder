'use client';

// Placeholder — implement read-only quiz detail rendering in the next phase.

export function QuizDetailView() {
  return (
    <div className="emptyState">
      <p>Quiz detail component — connect to GET /api/quizzes/:id.</p>
    </div>
  );
}
