'use client';

// Placeholder — implement create quiz form with React Hook Form in the next phase.

export function CreateQuizForm() {
  return (
    <div className="emptyState">
      <p>Create quiz form — connect to POST /api/quizzes.</p>
    </div>
  );
}
